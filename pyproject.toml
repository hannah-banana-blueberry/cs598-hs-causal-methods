[project]
name = "reading-room"
version = "1.0.0"
description = "Reading Room Flask survey app."
dependencies = [
    "flask",
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"